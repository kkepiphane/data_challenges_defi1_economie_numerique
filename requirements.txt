# ----------------------------------------------------------------------------
# DEPENDANCES DE L'APPLICATION — c'est ce fichier que Streamlit Cloud installe.
# ----------------------------------------------------------------------------
# Quatre bibliothèques, toutes en roues pures ou pré-compilées : le déploiement
# ne compile rien et démarre à froid en quelques secondes.
#
# L'application ne calcule RIEN. Elle affiche des fichiers déjà produits et
# contrôlés par la chaîne d'analyse. Elle n'a donc besoin ni de geopandas, ni
# de shapely, ni de pyproj, ni de scipy — voir `src/build_app_data.py`.
#
# Pour REJOUER l'analyse depuis les données brutes :  requirements-chaine.txt
# ----------------------------------------------------------------------------

streamlit==1.61.1      # application
pandas==2.3.3          # lecture et agrégation des tables
numpy==2.2.6           # repondération de l'indice, simulations de couverture
plotly==6.9.0          # graphiques et cartes interactifs
